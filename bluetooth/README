Intall need:
 sudo apt-get install build-essential
 sudo apt-get install libbluetooth-dev 
 sudo apt install pulseaudio-module-bluetooth 
 pulseaudio -k (kill pulseaudio. Maybe not need)
 pulseaudio --start
Compile:
 make
Re-Compile:
 make clean; make
